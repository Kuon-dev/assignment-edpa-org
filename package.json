{
	"dependencies": {
		"@unpic/react": "^0.1.8",
		"prettier": "^3.2.5"
	},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "lint": "npx prettier --write \"**/*.java\" --ignore-unknown --no-error-on-unmatched-pattern"
  },
	"devDependencies": {
		"prettier-plugin-java": "^2.5.0"
	}
}
